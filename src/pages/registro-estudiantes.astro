---
import i18next, { changeLanguage, t } from "i18next";
import LayoutStudent from "../layouts/LayoutStudent.astro";
import { sponsors } from "../data/constans_logos.js";
import MarqueeSponsors from "../components/register/MarqueeSponsors.astro";
import { RegisterStudent } from "@/components/register-student/RegisterStudent.jsx";

changeLanguage("es");

const registerTranslations = t("register", { returnObjects: true });
const currentLanguage = i18next.language;
---

<LayoutStudent title='ITM | registro estudiantes'>
  <main class='py-5 grid w-full content-center container mx-auto px-4 relative'>
    <section class='mb-10 max-w-screen-lg mx-auto'>
      <p class='text-center font-extrabold text-3xl mb-5'>
        {t('home.title-sponsors')}
      </p>
      <MarqueeSponsors partners={sponsors} isLeftToRight />
    </section>
    <h1 class='text-center font-extrabold text-4xl'>
      {t('register.title')}
    </h1>
    <p class='text-center text-2xl font-bold'>
      {t('register.description_student')}
    </p>
    <RegisterStudent
      translates={registerTranslations}
      currentLanguage={currentLanguage}
      client:only='react'
    />
  </main>
</LayoutStudent>
<style>
  body {
    background-color: #111827;
    color: white;
  }
</style>
